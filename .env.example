# Next.js Server & Environment
PORT=3000
NODE_ENV=development

# Database Connection (SQLite local file)
DATABASE_URL="file:./dev.db"

# JWT Authentication Secret
JWT_SECRET="greenview-society-tracker-jwt-secret-key-2026"

# Email Configuration (Optional - safe console logging used if unconfigured)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"
# SMTP_FROM='"Greenview Heights Maintenance" <maintenance@greenview.com>'
